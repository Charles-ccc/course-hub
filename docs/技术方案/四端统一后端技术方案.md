# 四端统一后端技术方案（商用版）

## 1. 文档目标

本方案用于指导后端服务按统一契约生成工程，覆盖以下客户端：

- web-admin（平台运营后台）
- web-institution（机构管理门户）
- h5-staff（业务员工作台）
- mp-student（学员小程序）

目标：

1. 提供可执行技术选型与模块边界。
2. 提供可直接生成后端骨架的 REST 接口与字段类型。
3. 明确状态机、错误码、审计与非功能约束，满足商用上线要求。

## 2. 技术选型

### 2.1 后端与基础设施

- Runtime: Node.js 22 LTS
- Framework: NestJS 11
- ORM: Prisma 6
- DB: MySQL 8.0
- Cache: Redis 7
- Auth: JWT（AccessToken）+ Refresh Token
- Validation: class-validator + class-transformer
- API 文档: Swagger(OpenAPI 3.1) 自动生成（源定义为 TypeScript DTO）
- 日志: pino（结构化 JSON）
- 追踪: OpenTelemetry（HTTP + DB + Redis）
- 定时任务: BullMQ / cron（结算、账务状态巡检）

### 2.2 前端约束（统一建议）

基础栈已确定：React + Ant Design。
建议补充库：

- Router: react-router-dom
- 请求层: axios（统一拦截器）
- 服务状态管理: @tanstack/react-query
- 全局轻状态: zustand
- 图表: echarts + echarts-for-react
- 日期: dayjs
- 表单增强: react-hook-form（可选，与 antd Form 二选一）

## 3. 统一工程结构（后端）

```text
apps/api/src
  |- modules
  |   |- auth
  |   |- admin
  |   |- institution
  |   |- staff
  |   |- student
  |   |- course
  |   |- order
  |   |- settlement
  |   |- report
  |   |- config
  |   |- notification
  |- common
  |   |- dto
  |   |- enums
  |   |- guards
  |   |- interceptors
  |   |- filters
  |   |- utils
  |- infra
  |   |- prisma
  |   |- redis
  |   |- sms-provider
  |   |- cos
```

## 4. 统一接口规范

### 4.0 统一访问入口（部署规范）

生产与预发环境应使用域名入口，不应长期使用 IP + 端口直连。

- web-admin: https://admin.happymaa.cn
- web-institution: https://institution.happymaa.cn
- h5-staff: https://staff.happymaa.cn
- api: https://api.happymaa.cn
- static: https://static.happymaa.cn

数据库管理入口（Adminer）不纳入公网标准入口，默认仅内网可达。

### 4.1 API 版本

- Base URL: /api/v1
- 破坏性变更必须升版本。

### 4.2 鉴权模型

- Header: Authorization: Bearer <token>
- Role 枚举：PLATFORM_ADMIN、INSTITUTION_ADMIN、STAFF、STUDENT
- 高风险接口需 RBAC + 操作审计。
- 登录接口必须具备防爆破策略：按 IP、手机号、账号维度做组合限流与失败计数。
- 连续登录失败达到阈值后，应触发短时冻结、验证码校验升级或风控拦截。
- 登录成功后应清空对应失败计数，避免误伤正常用户。

### 4.3 请求/响应包装

```ts
export interface ApiSuccess<T> {
  code: 0;
  message: "OK";
  requestId: string;
  timestamp: string; // ISO 8601 UTC
  data: T;
}

export interface ApiError {
  code: number; // 业务错误码
  message: string;
  requestId: string;
  timestamp: string;
  details?: Record<string, unknown>;
}

export interface PageQuery {
  page: number; // >=1
  pageSize: number; // 1~100
}

export interface PageResult<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}
```

### 4.4 字段规则

1. 金额字段：单位为分，类型 bigint（接口传 number，超过安全范围时传 string）。
2. 比率字段：百分数，最多两位小数（0~100）。
3. 时间字段：ISO 8601 UTC 字符串。
4. 状态字段：必须用枚举。
5. 主键：使用 string（推荐雪花 ID 或 ULID）。

## 5. 领域模型（逻辑层）

```ts
export interface Institution {
  id: string;
  name: string;
  socialCreditCode: string;
  status: InstitutionStatus;
  settlementRate: number; // 0~100, 两位小数
  depositBalanceCents: number;
  cumulativeGmvCents: number;
  cumulativeServiceFeeCents: number;
  createdAt: string;
  updatedAt: string;
}

export interface Course {
  id: string;
  institutionId: string;
  name: string;
  description: string;
  instructorInfo: string;
  priceCents: number;
  periodCount: number;
  status: CourseStatus;
  auditStatus: CourseAuditStatus;
  createdAt: string;
  updatedAt: string;
}

export interface Salesman {
  id: string;
  institutionId: string;
  username: string;
  name: string;
  phone: string;
  contractType: ContractType;
  status: SalesmanStatus;
  studentCount: number;
  cumulativeCommissionCents: number;
  createdAt: string;
  updatedAt: string;
}

export interface Student {
  id: string;
  phone: string;
  name?: string;
  realnameStatus: RealnameStatus;
  boundSalesmanId?: string;
  createdAt: string;
  updatedAt: string;
}

export interface Order {
  id: string;
  studentId: string;
  institutionId: string;
  courseId: string;
  totalAmountCents: number;
  status: OrderStatus;
  signStatus: SignStatus;
  createdAt: string;
  updatedAt: string;
}

export interface InstallmentPeriod {
  id: string;
  orderId: string;
  periodNo: number;
  dueDate: string;
  amountCents: number;
  status: PeriodStatus;
  overdueDays: number;
  createdAt: string;
  updatedAt: string;
}

export interface SettlementRecord {
  id: string;
  institutionId: string;
  period: string; // YYYY-MM
  gmvCents: number;
  serviceFeeCents: number;
  status: SettlementStatus;
  settledAt?: string;
  createdAt: string;
}

export interface SystemConfig {
  priceLimitCents: number;
  minAge: number;
  maxAge: number;
  zhimaEnabled: boolean;
}
```

## 6. REST 接口清单（可生成）

说明：以下接口均采用 /api/v1 前缀。

### 6.1 Auth 认证

| Method | Path                       | Auth | Client                              | 说明           |
| ------ | -------------------------- | ---- | ----------------------------------- | -------------- |
| POST   | /auth/phone/login          | 否   | web-institution,h5-staff,mp-student | 手机号密码登录 |
| POST   | /auth/alipay/login         | 否   | mp-student                          | 支付宝静默登录 |
| POST   | /auth/sms/send             | 否   | mp-student                          | 发送短信验证码 |
| POST   | /auth/phone/register       | 否   | mp-student                          | 手机号注册     |
| POST   | /auth/phone/reset-password | 否   | mp-student                          | 重置密码       |
| POST   | /auth/logout               | 是   | all                                 | 登出           |
| GET    | /auth/me                   | 是   | all                                 | 当前用户信息   |

```ts
export interface PhoneLoginReq {
  phone: string;
  password: string;
  clientType: "INSTITUTION_WEB" | "STAFF_H5" | "STUDENT_MP" | "ADMIN_WEB";
}

export interface LoginResp {
  accessToken: string;
  refreshToken: string;
  expiresIn: number;
  role: Role;
  userId: string;
  displayName: string;
}

export interface SendSmsReq {
  phone: string;
  type: "REGISTER" | "RESET_PASSWORD" | "LOGIN";
}

export interface RegisterReq {
  phone: string;
  code: string;
  password: string;
  orgCode?: string;
  inviteSalesmanId?: string;
}
```

### 6.2 Admin 平台运营

说明：本节以当前 web-admin 已落地前端为准，后端 Controller、DTO、Service 骨架应直接按以下路径与字段生成。

#### 6.2.0 Admin Auth

| Method | Path              | Auth | Client    | 说明           |
| ------ | ----------------- | ---- | --------- | -------------- |
| POST   | /admin/auth/login | 否   | web-admin | 用户名密码登录 |

```ts
export interface AdminLoginReq {
  username: string; // 3~32, 字母/数字/下划线
  password: string; // 8~64
}

export interface LoginResp {
  accessToken: string;
  refreshToken: string;
  expiresIn: number;
  role: Role;
  userId: string;
  displayName: string;
}
```

补充约束：

1. 登录失败统一返回 40001，提示“用户名或密码错误”，不区分账号不存在或密码错误。
2. 按 IP + username 维度记录失败次数，达到阈值后触发短时冻结。
3. 登录成功后清除对应失败计数并写入登录审计日志。

#### 6.2.1 Dashboard

| Method | Path                    | Auth           | Client    | 说明           |
| ------ | ----------------------- | -------------- | --------- | -------------- |
| GET    | /admin/dashboard/health | PLATFORM_ADMIN | web-admin | 平台健康度卡片 |

```ts
export interface DashboardHealthDto {
  totalGmvCents: number;
  activeOrderCount: number;
  repaymentRate: number; // 0~100
  overdueRate: number; // 0~100
  refundRate: number; // 0~100
  status: "HEALTHY" | "WARNING";
  warningMetrics: Array<"REPAYMENT_RATE" | "OVERDUE_RATE" | "REFUND_RATE">;
}
```

#### 6.2.2 Institution 管理

| Method | Path                            | Auth           | Client    | 说明         |
| ------ | ------------------------------- | -------------- | --------- | ------------ |
| GET    | /admin/institutions             | PLATFORM_ADMIN | web-admin | 机构列表     |
| POST   | /admin/institutions/:id/approve | PLATFORM_ADMIN | web-admin | 审核通过机构 |
| POST   | /admin/institutions/:id/suspend | PLATFORM_ADMIN | web-admin | 暂停机构     |

```ts
export interface AdminInstitutionQuery {
  status?: InstitutionStatus; // PENDING | ACTIVE | SUSPENDED | EXITED
}

export interface InstitutionDto {
  id: string;
  name: string;
  socialCreditCode: string;
  settlementRate: number;
  depositBalanceCents: number;
  cumulativeGmvCents: number;
  cumulativeServiceFeeCents: number;
  status: InstitutionStatus;
}

export interface ApproveInstitutionReq {
  settlementRate: number; // 0.00~100.00, 2位小数
}

export interface SuspendInstitutionReq {
  reason: string; // 1~200
}
```

补充约束：

1. `approve` 仅允许当前状态为 `PENDING` 的机构调用。
2. `suspend` 仅允许当前状态为 `ACTIVE` 的机构调用。
3. `approve` 成功后机构状态变更为 `ACTIVE`，并写入 `institution_status_logs` 审计记录。
4. `suspend` 成功后机构状态变更为 `SUSPENDED`，并将暂停原因写入审计记录。

#### 6.2.3 Course 管理

| Method | Path                       | Auth           | Client    | 说明              |
| ------ | -------------------------- | -------------- | --------- | ----------------- |
| GET    | /admin/courses             | PLATFORM_ADMIN | web-admin | 课程列表/审核列表 |
| POST   | /admin/courses/:id/offline | PLATFORM_ADMIN | web-admin | 下架课程          |
| POST   | /admin/courses/:id/approve | PLATFORM_ADMIN | web-admin | 审核通过课程      |
| POST   | /admin/courses/:id/reject  | PLATFORM_ADMIN | web-admin | 驳回课程          |

```ts
export interface AdminCourseQuery {
  auditStatus?: CourseAuditStatus; // APPROVED | PENDING_REVIEW | REJECTED
  institutionId?: string;
}

export interface AdminCourseDto {
  id: string;
  institutionId: string;
  institutionName: string;
  name: string;
  priceCents: number;
  status: CourseStatus;
  auditStatus: CourseAuditStatus;
  createdAt: string;
}

export interface RejectCourseReq {
  reason: string; // 1~200
}
```

补充约束：

1. `approve` 仅允许 `auditStatus=PENDING_REVIEW` 的课程调用，成功后同时设置 `auditStatus=APPROVED`、`status=ONLINE`。
2. `reject` 仅允许 `auditStatus=PENDING_REVIEW` 的课程调用，成功后设置 `auditStatus=REJECTED`、`status=OFFLINE`。
3. `offline` 仅允许当前 `status=ONLINE` 的课程调用，不影响已存在订单。

#### 6.2.4 Salesman 管理

| Method | Path                        | Auth           | Client    | 说明       |
| ------ | --------------------------- | -------------- | --------- | ---------- |
| GET    | /admin/salesmen             | PLATFORM_ADMIN | web-admin | 业务员列表 |
| POST   | /admin/salesmen             | PLATFORM_ADMIN | web-admin | 新增业务员 |
| POST   | /admin/salesmen/:id/disable | PLATFORM_ADMIN | web-admin | 禁用业务员 |

```ts
export interface AdminSalesmanQuery {
  status?: SalesmanStatus; // ACTIVE | DISABLED
}

export interface SalesmanDto {
  id: string;
  institutionId: string;
  name: string;
  phone: string;
  contractType: ContractType;
  status: SalesmanStatus;
  studentCount: number;
  cumulativeCommissionCents: number;
}

export interface CreateSalesmanReq {
  institutionId: string;
  username: string; // 3~32
  password: string; // 8~64
  name: string; // 1~50
  phone: string; // 中国大陆手机号
  contractType: ContractType; // EMPLOYEE | AGENT
}
```

补充约束：

1. 新增业务员时需校验所属机构状态为 `ACTIVE`。
2. `username`、`phone` 需在平台内唯一。
3. `disable` 成功后业务员状态改为 `DISABLED`，推广二维码立即失效。

#### 6.2.5 Reports 与 Settlement

| Method | Path                           | Auth           | Client    | 说明         |
| ------ | ------------------------------ | -------------- | --------- | ------------ |
| GET    | /admin/reports/gmv             | PLATFORM_ADMIN | web-admin | GMV 月报     |
| GET    | /admin/reports/overdue-monitor | PLATFORM_ADMIN | web-admin | 逾期率监控   |
| GET    | /admin/settlements             | PLATFORM_ADMIN | web-admin | 结算记录列表 |
| POST   | /admin/settlements/:id/execute | PLATFORM_ADMIN | web-admin | 执行结算     |

```ts
export interface GmvReportQuery {
  month?: string; // YYYY-MM, 不传默认当前月
}

export interface GmvReportDto {
  month: string;
  totalGmvCents: number;
  totalServiceFeeCents: number;
  items: Array<{
    institutionId: string;
    institutionName: string;
    gmvCents: number;
    serviceFeeCents: number;
  }>;
}

export interface OverdueMonitorDto {
  overdueRate: number;
  overdueInstallments: number;
  totalInstallments: number;
}

export interface SettlementQuery {
  period?: string; // YYYY-MM
  status?: SettlementStatus;
}

export interface SettlementRecordDto {
  id: string;
  institutionId: string;
  institutionName: string;
  period: string; // YYYY-MM
  gmvCents: number;
  serviceFeeCents: number;
  status: SettlementStatus;
  settledAt?: string;
}
```

补充约束：

1. `execute` 仅允许当前状态为 `PENDING` 的结算记录调用。
2. 成功后状态更新为 `SETTLED`，并记录 `settledAt` 与操作者信息。
3. 结算操作必须幂等，重复提交不得重复结算。

#### 6.2.6 System Config

| Method | Path                 | Auth           | Client    | 说明         |
| ------ | -------------------- | -------------- | --------- | ------------ |
| GET    | /admin/system-config | PLATFORM_ADMIN | web-admin | 获取系统配置 |
| PUT    | /admin/system-config | PLATFORM_ADMIN | web-admin | 更新系统配置 |

```ts
export interface SystemConfigDto {
  priceLimitCents: number; // 正整数
  minAge: number; // >= 0
  maxAge: number; // <= 120, 且 >= minAge
  zhimaEnabled: boolean;
}

export interface UpdateSystemConfigReq {
  priceLimitCents: number;
  minAge: number;
  maxAge: number;
  zhimaEnabled: boolean;
}
```

补充约束：

1. `priceLimitCents` 更新后对新下单实时生效，不回溯修改存量订单。
2. 当 `zhimaEnabled=false` 时，学员端若发起对应支付链路，应返回 `42202 CREDIT_PROVIDER_NOT_ENABLED`。
3. 配置变更必须写入配置变更审计表，记录变更前值、变更后值、操作者与 requestId。

### 6.3 Institution 机构端

说明：当前 web-institution 前端未单独请求“概览统计接口”，而是通过 `/institution/orders` + `/institution/deposit` 在前端本地聚合概览卡片与最近订单预览。后端优先保证以下接口契约稳定，再考虑追加独立 overview 接口。

| Method | Path                                      | Auth              | Client          | 说明                   |
| ------ | ----------------------------------------- | ----------------- | --------------- | ---------------------- |
| GET    | /institution/deposit                      | INSTITUTION_ADMIN | web-institution | 机构资料/保证金/费率   |
| GET    | /institution/orders                       | INSTITUTION_ADMIN | web-institution | 订单列表，附带分期明细 |
| GET    | /institution/courses                      | INSTITUTION_ADMIN | web-institution | 课程列表               |
| POST   | /institution/courses                      | INSTITUTION_ADMIN | web-institution | 创建课程               |
| PATCH  | /institution/courses/:id/status           | INSTITUTION_ADMIN | web-institution | 上下架                 |
| GET    | /institution/overdue/periods              | INSTITUTION_ADMIN | web-institution | 逾期列表               |
| POST   | /institution/overdue/periods/:id/writeoff | INSTITUTION_ADMIN | web-institution | 坏账核销               |
| GET    | /institution/settlements                  | INSTITUTION_ADMIN | web-institution | 结算记录               |
| GET    | /institution/questions                    | INSTITUTION_ADMIN | web-institution | 问题列表               |
| POST   | /institution/questions/:id/reply          | INSTITUTION_ADMIN | web-institution | 回复问题               |

```ts
export interface InstitutionDepositDto {
  orgId?: string;
  orgName?: string;
  settlementRate: number;
  depositBalanceCents: number;
}

export interface CreateCourseReq {
  name: string;
  description: string;
  instructorInfo: string;
  priceCents: number;
  periodCount: number;
}

export interface CourseDto {
  id: string;
  name: string;
  description: string;
  instructorInfo: string;
  priceCents: number;
  periodCount: number;
  status: CourseStatus;
}

export type InstitutionCourseListResp = PageResult<CourseDto>;

export interface UpdateCourseStatusReq {
  status: CourseStatus;
}

export interface InstitutionOrderQuery {
  status?: OrderStatus;
}

export interface InstallmentDto {
  id: string;
  periodNo: number;
  dueDate: string;
  amountCents?: number;
  plannedAmountCents?: number;
  paidAmountCents?: number;
  contentDeliveredAt?: string;
  status: PeriodStatus;
}

export interface OrderDto {
  id: string;
  studentName: string;
  courseName: string;
  totalAmountCents: number;
  periodCount: number;
  status: OrderStatus;
  coolingOffEndAt?: string;
  createdAt: string;
  installments?: InstallmentDto[];
}

export type InstitutionOrderListResp = PageResult<OrderDto>;

export interface OverduePeriodDto {
  id: string;
  orderId: string;
  studentName: string;
  courseName: string;
  periodNo: number;
  dueDate: string;
  plannedAmountCents?: number;
  amountCents?: number;
  status: "OVERDUE" | "WRITTEN_OFF";
}

export type InstitutionOverdueListResp = PageResult<OverduePeriodDto>;

export interface WriteoffReq {
  remark: string;
}

export interface SettlementRecordDto {
  id: string;
  period: string;
  gmvCents: number;
  serviceFeeCents: number;
  status: SettlementStatus;
  settledAt?: string;
}

export type InstitutionSettlementListResp = PageResult<SettlementRecordDto>;

export interface InstitutionQuestionQuery {
  onlyPending?: boolean;
}

export interface QaQuestionDto {
  id: string;
  studentName: string;
  content: string;
  askedAt: string;
  replied: boolean;
  replyContent?: string;
  repliedAt?: string;
}

export type InstitutionQuestionListResp = PageResult<QaQuestionDto>;

export interface ReplyQuestionReq {
  content: string;
}
```

补充约束：

1. `/institution/orders` 当前需直接返回订单及其 `installments`，以满足前端概览和订单展开明细两种展示，不再额外依赖 `/institution/orders/:id`。
2. `/institution/courses`、`/institution/orders`、`/institution/overdue/periods`、`/institution/settlements`、`/institution/questions` 均允许返回 `PageResult<T>` 或直接返回数组；若返回 `PageResult<T>`，前端当前仅消费 `items`。
3. 课程创建成功后默认 `status=OFFLINE`；若 `priceCents` 超出平台限额，返回 `40002 PRICE_LIMIT_EXCEEDED`。
4. `/institution/overdue/periods/:id/writeoff` 成功后对应期次状态变更为 `WRITTEN_OFF`，且不得影响同订单其他期次。
5. 学员姓名由后端返回原值，机构端前端统一脱敏展示；后端不在 DTO 层做姓名脱敏。
6. 若后续补建独立概览接口，应保持与现有前端本地聚合结果一致，不得改变现有列表接口字段语义。

### 6.4 Staff 业务员端

| Method | Path                | Auth  | Client   | 说明     |
| ------ | ------------------- | ----- | -------- | -------- |
| GET    | /staff/home/summary | STAFF | h5-staff | 首页汇总 |
| GET    | /staff/home/qrcode  | STAFF | h5-staff | 邀请码   |
| GET    | /staff/students     | STAFF | h5-staff | 学员列表 |
| GET    | /staff/commissions  | STAFF | h5-staff | 提成明细 |
| GET    | /staff/profile      | STAFF | h5-staff | 个人信息 |

```ts
export interface StaffStudentsQuery extends PageQuery {
  tab?: "ALL" | "DUE_IN_7_DAYS" | "DUE_TODAY" | "OVERDUE";
}

export interface StaffHomeSummaryResp {
  settledCommissionCents: number;
  pendingCommissionCents: number;
  heldCommissionCents: number;
  studentCount: number;
}
```

### 6.5 Student 学员端

| Method | Path                         | Auth    | Client     | 说明         |
| ------ | ---------------------------- | ------- | ---------- | ------------ |
| POST   | /student/realname/verify     | STUDENT | mp-student | 实名认证     |
| GET    | /student/courses             | STUDENT | mp-student | 课程列表     |
| GET    | /student/courses/:id         | STUDENT | mp-student | 课程详情     |
| GET    | /student/courses/:id/videos  | STUDENT | mp-student | 课程视频     |
| GET    | /student/videos/:videoId/url | STUDENT | mp-student | 播放地址     |
| POST   | /student/orders              | STUDENT | mp-student | 创建订单     |
| GET    | /student/orders              | STUDENT | mp-student | 我的订单     |
| GET    | /student/orders/:id          | STUDENT | mp-student | 订单详情     |
| POST   | /student/orders/:id/sign     | STUDENT | mp-student | 电子签约     |
| POST   | /student/learning/checkin    | STUDENT | mp-student | 人脸打卡     |
| GET    | /student/me                  | STUDENT | mp-student | 个人中心信息 |

```ts
export interface StudentCourseQuery extends PageQuery {
  keyword?: string;
  institutionId?: string;
}

export interface CreateOrderReq {
  courseId: string;
  channel: "ALIPAY";
}

export interface RealnameVerifyReq {
  name: string;
  idNo: string;
  phone: string;
}

export interface CheckinReq {
  orderId: string;
  coursePeriod: number;
  faceToken: string;
}
```

## 7. 枚举定义

```ts
export type Role = "PLATFORM_ADMIN" | "INSTITUTION_ADMIN" | "STAFF" | "STUDENT";

export type InstitutionStatus = "PENDING" | "ACTIVE" | "SUSPENDED" | "EXITED";

export type CourseStatus = "ONLINE" | "OFFLINE";

export type CourseAuditStatus = "PENDING_REVIEW" | "APPROVED" | "REJECTED";

export type SalesmanStatus = "ACTIVE" | "DISABLED";

export type ContractType = "EMPLOYEE" | "AGENT";

export type RealnameStatus = "UNVERIFIED" | "VERIFIED" | "REJECTED";

export type OrderStatus =
  | "CREATED"
  | "COOLING_OFF"
  | "ACTIVE"
  | "COMPLETED"
  | "TERMINATED"
  | "REFUNDED";

export type SignStatus = "UNSIGNED" | "SIGNED" | "SIGN_FAILED";

export type PeriodStatus =
  | "PENDING"
  | "DELIVERED"
  | "PAID"
  | "OVERDUE"
  | "WRITTEN_OFF";

export type SettlementStatus = "PENDING" | "SETTLED";

export type CommissionStatus = "PENDING" | "SETTLED" | "HELD" | "CLAWED_BACK";
```

## 8. 状态机约束

1. Institution: PENDING -> ACTIVE -> SUSPENDED -> EXITED（不允许反向恢复，恢复需新审批流程）。
2. Salesman: ACTIVE -> DISABLED（当前版本不可恢复）。
3. Settlement: PENDING -> SETTLED（不可撤销）。
4. Order: CREATED -> COOLING_OFF -> ACTIVE -> COMPLETED，任意阶段可进入 TERMINATED/REFUNDED。
5. Period: PENDING -> DELIVERED -> PAID，DELIVERED 可超时转 OVERDUE，OVERDUE 可核销为 WRITTEN_OFF。

## 9. 错误码（首版）

```ts
export enum ErrorCode {
  // Auth
  INVALID_CREDENTIALS = 40001,
  UNDERAGE_USER = 40002,
  SMS_CODE_INVALID = 40003,
  TOKEN_EXPIRED = 40101,
  FORBIDDEN = 40301,

  // Institution
  INSTITUTION_NOT_FOUND = 41001,
  INSTITUTION_ALREADY_APPROVED = 41002,
  INSTITUTION_NOT_ACTIVE = 41003,

  // Course
  COURSE_NOT_FOUND = 42001,
  COURSE_PRICE_LIMIT_EXCEEDED = 42002,
  COURSE_REVIEW_CONFLICT = 42003,

  // Order & Repayment
  ORDER_NOT_FOUND = 43001,
  ORDER_SIGN_FAILED = 43002,
  PERIOD_NOT_FOUND = 43003,
  PERIOD_ALREADY_WRITTEN_OFF = 43004,

  // Settlement
  SETTLEMENT_NOT_FOUND = 44001,
  SETTLEMENT_ALREADY_DONE = 44002,

  // Provider
  CREDIT_PROVIDER_NOT_ENABLED = 42202,
  SMS_PROVIDER_UNAVAILABLE = 45001,

  // System
  REQUEST_VALIDATION_FAILED = 46000,
  INTERNAL_SERVER_ERROR = 50000,
}
```

## 10. 审计、安全与可观测

1. 审计日志

- 高风险接口必须记录：operatorId、role、action、resourceId、before、after、reason、requestId。

2. 安全要求

- 登录与短信接口按 IP + phone 双维限流。
- 密码登录建议增加分级风控：5 次失败触发 15 分钟冷却，10 次失败触发人工解锁或更强验证。
- 管理端登录建议记录设备指纹、UA、来源 IP 与失败原因，用于审计与异常告警。
- 敏感字段加密存储（如身份证号脱敏存储）。
- 所有写接口启用参数校验与 XSS/SQL 注入防护。

3. 可观测

- 每个请求注入 requestId。
- 指标至少包含：QPS、P95、错误率、第三方依赖成功率。
- 对短信、签约、人脸服务设置失败告警阈值。

## 11. 工程生成输入说明

以下章节可直接作为后端脚手架输入：

1. 第 4 章统一接口规范：生成通用响应、分页 DTO。
2. 第 6 章 REST 接口清单：生成 Controller 路由、请求 DTO、响应类型。
3. 第 7 章枚举定义：生成 enums 与 Prisma enum 映射。
4. 第 9 章错误码：生成错误码常量与异常映射器。

需要人工补全：

1. 第三方 SDK 适配器实现（短信、签约、人脸）。
2. 复杂结算任务编排与补偿逻辑。
3. 生产环境容量规划与压测参数。

## 12. 里程碑建议（商用）

1. M1（2 周）：认证、机构、课程、基础订单链路打通。
2. M2（2 周）：结算、报表、业务员提成、逾期核销。
3. M3（2 周）：短信、签约、人脸与全链路审计告警。
4. M4（1 周）：联调验收、压测、上线演练与回滚预案。
