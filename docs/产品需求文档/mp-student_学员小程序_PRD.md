# mp-student · 学员小程序 PRD

> **版本：v2.0** | 更新日期：2026-06-16  
> 本文档按三个迭代阶段组织，Iteration 1 为 MVP，Iteration 2 为核心功能完善，Iteration 3 为高级合规与体验升级。

---

# 1. 背景

学员小程序是「欢乐学」面向学员的核心消费端，目前仅支持支付宝小程序。
前端技术实现采用支付宝原生小程序方案，不使用跨端框架。

学员通过业务员的邀请链接或二维码进入小程序，完成注册、实名认证、浏览课程、下单签约、分期还款和在线学习的完整消费旅程。「先学后付」是本产品最核心的商业模式：学员先学习本期课程，课程结束后系统自动代扣学费，降低学员的决策门槛。

## 1.1 功能范围

- 归因追踪：在应用启动时捕获来源（业务员/学员邀请），写入归因数据
- 注册与认证：支付宝授权注册（手机号免填）、登录页支付宝授权登录、支付宝开放认证实名
- 课程发现：关键词搜索、课程列表、课程详情和免费试学
- 下单与签约：创建订单、支付宝核身签约确认（Iter2）
- 学习：视频课播放（Iter2）、联系老师、人脸打卡获取学习激励（Iter3）
- 订单管理：查看订单列表、学费分期支付进度
- 个人中心：查看个人信息、实名认证入口、退出登录

## 1.2 价值

对学员：先体验后付款，降低购课决策风险；电子签约保障权益透明。  
对平台：归因追踪保障业务员提成准确计算；实名认证和合同签署构建合规基础。

---

# 2. 迭代规划总览

| 迭代               | 目标                     | 核心范围                                                                                              |
| ------------------ | ------------------------ | ----------------------------------------------------------------------------------------------------- |
| Iteration 1（MVP） | 跑通最小闭环，可内测上线 | 归因、支付宝注册/登录、支付宝核身实名、课程发现、下单流程（签约占位）、订单列表、个人中心、学习页骨架 |
| Iteration 2        | 完成签约与学习主链路     | 支付宝核身电子签约、视频播放（点播对接）、分期明细                                                    |
| Iteration 3        | 激励完善                 | 人脸打卡激励                                                                                          |

---

# 3. 产品功能清单

| 功能模块 | 功能点           | 功能描述                                                | 迭代  |
| -------- | ---------------- | ------------------------------------------------------- | ----- |
| 启动     | 归因参数捕获     | 启动时从场景值解析 staffId 或 studentId，写入本地存储   | Iter1 |
| 启动     | 登录页授权登录   | 未登录用户启动小程序显示登录页，授权后已注册直接进首页 | Iter1 |
| 启动     | 引导页           | 未注册时展示注册入口                                    | Iter1 |
| 注册     | 支付宝授权注册   | 输入机构码后，通过 getPhoneNumber 授权获取手机号完成注册 | Iter1 |
| 实名认证 | 支付宝开放认证   | 调用 alipay.user.certify.open.* 完成人脸+身份证核验     | Iter1 |
| 课程发现 | 课程列表         | 关键词搜索课程，分页加载                                | Iter1 |
| 课程发现 | 课程详情         | 展示课程信息、分期方案，支持免费试学入口                | Iter1 |
| 下单     | 创建订单         | 确认课程和付款方式，提交订单                            | Iter1 |
| 下单     | 先学后付订单     | 需机构审核通过后生效                                    | Iter1 |
| 下单     | 电子签约（占位） | 签约按钮占位，点击提示即将上线                          | Iter1 |
| 下单     | 电子签约（实现） | 通过支付宝核身完成本人确认签约                          | Iter2 |
| 学习     | 视频播放页骨架   | 搭建播放页，展示章节列表，播放区域占位                  | Iter1 |
| 学习     | 视频播放（实现） | 对接点播服务，支持试学与完整播放                        | Iter2 |
| 学习     | 联系老师         | 展示客服联系方式（微信/电话）                           | Iter1 |
| 学习     | 人脸打卡         | 支付宝核身打卡，获取学习激励（抵扣学费）                | Iter3 |
| 订单管理 | 订单列表         | 查看全部购课记录及状态                                  | Iter1 |
| 订单管理 | 订单详情         | 查看学费分期明细和当前期次状态                          | Iter1 |
| 个人中心 | 个人信息         | 展示实名认证状态、手机号                                | Iter1 |
| 个人中心 | 退出登录         | 清空 token 并返回首页                                   | Iter1 |

---

# 4. Iteration 1 — MVP 需求

## 4.0 需求背景与业务目标

### 4.0.1 背景

为保障用户来源可控，避免无效注册，平台采用**业务员邀请制注册**模式。用户必须通过**机构码 / 业务员邀请码**完成注册，注册后支持多种登录方式。所有注册用户必须归属某一业务员或机构，作为后续业绩统计、提成计算的数据基础。

### 4.0.2 业务目标

- 所有注册用户必须绑定业务员，确保用户来源可控
- 防止非目标用户随意注册，降低无效流量
- 记录完整的邀请关系链，支撑业绩追踪与提成计算
- 降低登录门槛，支持多种登录方式，提升留存率
- 提升注册转化率，缩短注册链路

### 4.0.3 用户角色

| 角色       | 说明                                                 |
| ---------- | ---------------------------------------------------- |
| 普通学员   | 被邀请注册的小程序用户，通过业务员邀请码完成注册     |
| 业务员     | 拥有机构码和邀请码，负责邀请学员，后续与学员提成挂钩 |
| 后台管理员 | 管理业务员、机构码状态、邀请关系审核                 |

### 4.0.4 业务流程（文字版）

**【未注册学员】**

```
打开小程序 → [自动捕获 staffId]
→ my.login() 获取 authCode → 后端查 openId → 未注册
→ 展示注册引导页
→ 用户输入机构码 → 后端校验机构码
→ 展示机构信息 + 业务员名称
→ 用户点击「授权手机号」（支付宝原生授权组件）
→ 后端解密获得手机号 → 创建账号 → 绑定业务员
→ 跳转实名认证页（支付宝人脸核身）
```

**【已注册学员】**

```
打开小程序 → my.login() 获取 authCode（用户无感知）
→ POST /auth/alipay/login { authCode }
→ 后端查 openId：已注册 → 直接返回 token → 跳首页
→ token 过期时自动刷新，无需用户操作
```

---

## 4.1 应用启动与归因

作为平台，我希望在学员首次进入小程序时自动捕获来源归因，以便准确记录学员与业务员的绑定关系和学员互邀关系。

验收条件：

1. 应用启动时（onLaunch），解析启动参数中的 `?staff=<staffId>` 字段，若存在则写入本地存储键 `referrerStaffId`。
2. 启动参数中含 `?inv=<studentId>` 时，写入本地存储键 `referrerStudentId`。
3. 归因数据在后续注册请求时一并传入，其中 `referrerStaffId` 透传为 `inviteSalesmanId`；后端仅在首次注册成功时写入绑定关系，已绑定关系不可被后续请求覆盖。
4. 启动时检测本地是否有 accessToken：
   - 有 → 直接跳首页 Tab；后续 401 时跳转登录页
   - 无 → 跳转登录页 `/pages/auth/login/index`
5. 登录页用户主动点击「支付宝授权登录」按钮，触发 `my.getAuthCode` → `POST /api/v1/auth/alipay/login { authCode }`：
   - 已注册 → 保存 token，跳首页 Tab
   - 未注册 → 跳引导页 → 注册页

---

## 4.2 注册功能

### 4.2.1 机构码校验

作为学员，我希望通过输入机构码完成身份验证，以便确保我被正确的机构和业务员邀请。

验收条件：

**校验流程**

1. 注册表单首项为「机构码」输入框，用户输入机构码后，实时请求 `POST /api/v1/auth/org-code/validate { orgCode }`。
2. 校验项包括：机构码是否存在、是否启用（status = ACTIVE）、是否过期（expireAt > now）。
3. 校验成功后，页面展示确认卡片：「您将加入：XX 机构（业务员：张三）」，展示机构名称和关联业务员信息。
4. 校验失败时，根据错误码展示对应提示（见 4.7 异常处理）。

**限流与防暴力**

5. 同一支付宝 openId 在 24 小时内机构码校验失败 ≥ 5 次，冻结该账号机构码输入框 30 分钟；冻结期间提示「尝试次数过多，请 30 分钟后重试」。
6. 冻结记录存储至后端，端上不得绕过限流。

---

### 4.2.2 支付宝手机号授权注册

作为学员，我希望通过支付宝一键授权手机号完成注册，无需手动输入手机号或验证码。

验收条件：

**注册流程**

1. 机构码校验通过后，页面展示「授权手机号并注册」按钮（使用支付宝原生 `<button open-type="getAuthorize" scope="phoneNumber">` 组件）。
2. 用户点击按钮后，支付宝弹出授权确认框；授权成功后前端获得加密的手机号数据（`encryptedData` + `iv`），传给后端解密。
3. 用户拒绝授权时，提示：**需要授权手机号才能完成注册**，按钮保持可点击状态。
4. 后端解密手机号后，校验该手机号是否已注册：
   - 已注册（同一 openId）→ 直接返回 token，完成登录
   - 已注册（不同 openId）→ 返回错误码 40004，提示：**该手机号已被其他账号绑定，请联系客服**
   - 未注册 → 创建新账号

**提交与结果**

5. 提交 `POST /api/v1/auth/alipay/register { authCode, encryptedData, iv, orgCode, inviteSalesmanId }`，其中 `inviteSalesmanId` 来自本地存储的 `referrerStaffId`。
6. 注册成功后，后端自动写入邀请关系（student → inviteSalesmanId）；已绑定关系不可被后续请求覆盖。
7. 注册成功后将 token 和 profile 写入本地存储，跳转至实名认证页（`/pages/auth/realname`）。
8. 注册失败时页面展示接口返回的错误信息。

---

## 4.3 登录功能

> 登录完全依托支付宝身份体系，无需密码、无需短信，用户无感知完成登录。

### 4.3.1 支付宝授权登录页

验收条件：

1. 启动小程序时，splash 检测本地是否有 accessToken。无 token 跳登录页 `/pages/auth/login/index`；有 token 直接进首页 Tab（401 时再回登录页）。
2. 登录页 UI：logo + 品牌名 + 单一「支付宝授权登录」按钮 + 用户协议链接。
3. 点击按钮调用 `my.getAuthCode({ scopes: 'auth_base' })` 拿 authCode，请求 `POST /api/v1/auth/alipay/login { authCode }`：
   - 已注册用户 → 后端返回 `accessToken` + `refreshToken` + `profile`，保存 token 后跳首页 Tab
   - 未注册用户 → 后端返回 `needRegister: true`，跳转注册引导页
4. 用户拒绝授权时提示「需要支付宝授权才能登录」，按钮可点击重试。
5. Access token 过期时，自动用 refresh token 换新 token，无感重试；refresh token 也过期则清空本地 token 并跳回登录页。

---

## 4.4 找回密码

> 本产品不使用密码体系，登录完全通过支付宝身份凭证完成，**找回密码功能不适用，本节已废除**。
> 
> 若学员更换支付宝账号等特殊情况，需通过客服人工处理。

---

## 4.5 邀请关系管理与数据记录

验收条件：

1. 每个注册用户绑定的业务员信息存储至后端数据库，字段：`userId`、`phone`、`inviteSalesmanId`、`inviteSalesmanName`、`registeredAt`、`sourceType`（STAFF_CODE / STUDENT_INVITE）。
2. 邀请关系在注册时一次性确定，后续不可修改。
3. 若注册时未提供 `inviteSalesmanId`，注册仍允许通过，该用户标记为「无来源」。
4. 已绑定的邀请关系不可被后续请求覆盖。

---

## 4.6 业务规则与限制

### 4.6.1 注册限制

| 规则                     | 说明                                           |
| ------------------------ | ---------------------------------------------- |
| 无机构码不可进入注册流程 | 注册首页强制机构码校验，校验失败不显示后续字段 |
| 禁止同一手机号重复注册   | 若手机号已注册，拦截并引导登录                 |
| 一个机构码可邀请无限用户 | 可在后台配置单个机构码的邀请上限（可选）       |
| 邀请关系在注册时确定     | 已绑定关系不可被后续请求覆盖                   |

### 4.6.2 登录限制

| 规则             | 说明                                         |
| ---------------- | -------------------------------------------- |
| 唯一登录方式     | 支付宝 authCode 授权登录，用户在登录页主动触发 |
| 无密码体系       | 不存在密码、重置密码等概念                   |
| Token 有效期     | access token 15 分钟，refresh token 7 天     |
| Refresh 过期处理 | 重新调用 my.login() 获取新 authCode 完成续期 |

---

## 4.7 异常处理

### 4.7.1 注册异常

| 错误码 | HTTP 状态 | 提示文案                               | 用户操作                             |
| ------ | --------- | -------------------------------------- | ------------------------------------ |
| 40001  | 400       | 机构码不存在或已失效                   | 返回注册页，重新输入机构码           |
| 40002  | 400       | 机构码已过期                           | 同上                                 |
| 40003  | 400       | 该机构码暂未启用                       | 同上                                 |
| 40004  | 400       | 该手机号已被其他账号绑定，请联系客服   | 提示联系客服                         |
| 40007  | 400       | 机构码校验失败次数过多，已冻结 30 分钟 | 页面禁用机构码输入框，显示冻结倒计时 |
| 40008  | 400       | 手机号授权失败，请重试                 | 重新点击授权按钮                     |
| 50000  | 500       | 注册失败，请稍后重试                   | 显示重试按钮                         |

### 4.7.2 登录异常

| 错误码 | HTTP 状态 | 提示文案             | 用户操作                           |
| ------ | --------- | -------------------- | ---------------------------------- |
| 40101  | 400       | 登录失败，请重试     | 重新触发 my.login()                |
| 40102  | 400       | 账号不存在，请先注册 | 跳转注册引导页                     |
| 50000  | 500       | 登录失败，请稍后重试 | 显示重试按钮                       |

---

## 4.8 实名认证（支付宝开放认证）

> 依托支付宝「支付宝身份验证」能力（`alipay.user.certify.open.*`），完成真实姓名 + 身份证 + 活体人脸三要素核验，无需用户手动填写任何表单。

验收条件：

1. 实名认证页展示说明文案：**需要完成实名认证才能购课，认证信息由支付宝安全核验**，以及「开始认证」按钮。
2. 点击「开始认证」，前端请求后端接口（`POST /users/realname/initialize`），后端依次调用：
   - `alipay.user.certify.open.initialize` → 创建认证流程，获取 `certifyId`
   - `alipay.user.certify.open.certify` → 启动认证，获取 `certifyUrl`
   - 将 `certifyUrl` 返回前端
3. 前端拿到 `certifyUrl` 后，通过 `my.ap.navigateToAlipayPage` 或 WebView 组件跳转到支付宝认证页面，用户在支付宝内完成活体检测 + 身份证比对。
4. 认证完成后，支付宝回调或用户返回小程序，前端调用后端（`POST /users/realname/confirm { certifyId }`），后端调用 `alipay.user.certify.open.query` 查询结果：
   - **认证通过**：`realnameStatus` 设为 `VERIFIED`，记录脱敏姓名和身份证后四位；前端 toast：**实名认证完成**，1.5 秒后跳转首页 Tab
   - **认证未通过**：提示：**认证未通过，请重新尝试**，提供「重新认证」按钮
   - **用户取消**：提示：**认证已取消**，按钮恢复可点击
5. 若后端从认证结果中判定年龄不满 18 周岁，返回错误码 40001（UNDERAGE_USER），页面展示：**未满 18 周岁，暂不可报名分期课程**，按钮保持禁用。
6. 实名认证通过后，个人中心展示「✓ 已实名」标识，该入口不再作为待完成提醒。
7. 已认证用户再次进入实名页时，展示已认证状态，不可重复提交。

---

## 4.9 课程发现

### 4.9.1 课程列表（首页）

验收条件：

1. 首页顶部提供搜索框，支持输入关键词后点击搜索或键盘确认触发搜索。
2. 搜索或首次加载时请求 `GET /api/v1/courses?keyword=<关键词>&page=1&pageSize=20`，响应统一 `{ items, page, pageSize, total, hasNext }`。
3. 课程卡片展示字段：封面（imageUrl 缺省时显示占位图）、课程名称、机构名称、每期价格（元/期，计算：priceCents / periodCount / 100）、课程总价（元）、「先学后付」徽标。
4. 列表为空时展示空状态提示。
5. 进入小程序前必须登录（未登录由 splash 跳转登录页），首页不存在「未登录浏览」状态。
6. 点击课程卡片跳转至课程详情页。

---

### 4.9.2 课程详情

验收条件：

**页面内容**

1. 展示字段：课程名称、机构名称、课程总价（元）、每期金额（元）、分期期数、课程大纲（文本）、合规声明（平台为居间方，机构为合同甲方）。
2. 页面底部提供「免费试学」和「立即报名」按钮。
3. 底部提供付款方式选择：立即付款 / 先学后付（单选，默认「立即付款」）。

**免费试学（Iter 1 占位）**

4. 点击「免费试学第一节」，跳转至学习页，传入参数 `trial=1`；Iter 1 阶段学习页展示章节列表骨架与占位提示「视频功能即将上线」；Iter 2 补全实际播放。

**购课确认**

5. 点击「立即报名」时，若用户尚未看过购课须知弹框（`trialConfirmed = false`），弹出购课须知 Modal，文案：**购课须知：购买后不以任何理由退课，请确认后提交**。
6. 学员在 Modal 内点击「确认」后，`trialConfirmed` 置为 true，跳转至下单确认页（携带 courseId 和 payType）。
7. 若 `trialConfirmed` 已为 true，直接跳转至下单确认页，不再弹窗。
8. `trialConfirmed` 为页面内本地状态，页面刷新后重置，不做持久化。

---

## 4.10 下单

### 4.10.1 下单确认

验收条件：

1. 下单确认页展示课程名称和所选付款方式说明：
   - 立即付款：一次性付款，立即开始学习
   - 先学后付：先学后付，每期课程结束后再扣款
2. 页面底部按钮文案：立即付款 → 「确认付款」；先学后付 → 「确认并签约」。
3. 点击确认按钮后，调用 `POST /api/v1/orders { courseId, payType }`，成功后 toast 提示，1.5 秒后跳转至订单详情页。
4. 若后端返回错误码 40002（PRICE_LIMIT_EXCEEDED），提示：**该课程暂不支持分期购买**。
5. 若后端返回错误码 40001（UNDERAGE_USER），提示：**未满 18 周岁，暂不可报名分期课程**。
6. 先学后付订单需机构确认后才生效，机构认为学员不满足条件时可在订单生效前取消订单。

---

### 4.10.2 电子签约（Iter 1 占位，Iter 2 实现）

> **当前阶段（Iter 1）：** 订单详情页展示「签约授权（法大大）」按钮，点击后 Toast 提示「签约功能即将上线，敬请期待」，不调用法大大接口。  
> **Iter 2 实现：** 对接法大大 H5 签约 SDK，补充 `POST /api/v1/orders/<orderId>/sign/fadadada` 后端实现及前端 WebView 跳转逻辑；接口路径与前端按钮已在 Iter 1 确定，升级无破坏性变更。

---

## 4.11 学习中心

### 4.11.1 视频播放（Iter 1 骨架，Iter 2 实现）

> **当前阶段（Iter 1）：** 搭建学习页骨架，展示章节列表结构，播放区域展示占位提示；接口路径已定义，前端组件已挂载，播放调用逻辑置空。  
> **Iter 2 实现：** 补充 `GET /api/v1/courses/videos/<videoId>/url` 后端实现，对接点播服务（腾讯云 VOD 或阿里云点播），前端播放区域替换为原生 `<video>` 组件并传入签名 URL；结构无破坏性变更。

验收条件（Iter 1）：

1. 学习页加载时，请求 `GET /api/v1/courses/<courseId>/videos` 获取视频章节列表。
2. 页面展示章节列表（标题、时长字段占位）。
3. 点击章节时 Toast 提示：**视频功能即将上线，敬请期待**。
4. 试学模式（URL 参数 `trial=1`）下，仅展示第一节，页面顶部展示黄色横幅：**试学模式 · 仅展示第一节课程**。
5. 试学模式下，页面底部展示报名引导卡片，提供购课入口。

---

### 4.11.2 联系老师

验收条件：

1. 展示客服联系方式，包含微信号和联系电话。

---

### 4.11.3 人脸打卡（Iter 3 方案预设，Iter 1/2 不展示入口）

> **当前阶段（Iter 1、2）：** 打卡入口不展示，相关代码预留注释占位。

**Iter 3 技术方案（预设）**

| 环节     | 说明                                                                           |
| -------- | ------------------------------------------------------------------------------ |
| 前端调用 | 生产环境调用 `my.startFaceVerify`，开发/测试环境使用 mock faceToken            |
| 后端接口 | `POST /api/v1/learning/checkin { orderId, coursePeriod, faceToken }`           |
| 后端逻辑 | 接收 faceToken，调用支付宝人脸核验服务验证身份，与实名信息比对                 |
| 激励发放 | 比对通过后，按后台配置（默认 ¥5/次，单位：分）写入学费抵扣记录                 |
| 打卡限制 | 单订单单期次最多打卡 N 次（后台可配置，默认 3）                                |
| 失败处理 | 比对不通过时展示失败状态，提供「再次打卡」按钮                                 |
| 次数超限 | 后端返回错误码 42001（CHECKIN_LIMIT_EXCEEDED），前端提示「今日打卡次数已用完」 |

---

## 4.12 订单管理

### 4.12.1 订单列表

验收条件：

1. 进入订单列表页时，若未登录则跳转至登录页。
2. 加载 `GET /api/v1/orders`，展示全部订单，按创建时间倒序排列。
3. 每张订单卡片展示：课程名称、机构名称、订单状态、总金额（元）、分期期数、已付期数。
4. 订单状态标签配色：CREATED（待签约，琥珀色）、ACTIVE（学习中，绿色）、COMPLETED（已完成，灰色）、TERMINATED（已终止，红色）。
5. 无订单时展示空状态提示，并提供「去看看课程」链接跳转至首页。
6. 点击订单卡片跳转至订单详情页。

---

### 4.12.2 订单详情与分期明细

验收条件：

1. 订单详情页展示：课程名称、签约/收款方（机构名称）、订单总金额（元）、分期期数、每期金额（元）。
2. 订单状态为 ACTIVE 或 COMPLETED 时，展示分期还款计划列表。
3. 每条分期记录展示：期次（第 X 期）、到期日、金额（元）、状态标签。
4. 分期状态展示规则：PAID → 已还款；OVERDUE → 逾期；DELIVERED → 待扣款；PENDING → 待解锁。
5. 操作区根据订单状态展示对应操作：
   - CREATED → 「签约授权」按钮（Iter 1 点击提示即将上线，Iter 2 通过支付宝核身完成签约）
   - ACTIVE → 「去学习」按钮，点击跳转至学习中心
   - COMPLETED → 展示静态文字「课程已完成」，无按钮

---

## 4.13 个人中心

验收条件：

**已登录状态**

1. 展示头像（取真实姓名首字）、真实姓名、手机号。
2. 若 `profile.realnameVerified = true`，在姓名旁展示「✓ 已实名」徽标。
3. 若 `profile.realnameVerified = false`，展示「完成实名认证」菜单项（橙色警告样式），点击跳转至实名认证页。
4. 提供「我的课程」菜单项，点击跳转至订单列表页。
5. 「退出登录」按钮点击后，清空本地存储的 token 和 profile，跳转至引导页。

**未登录状态**

6. 未登录时，个人中心显示提示文字：**登录后查看个人信息**，并提供「去登录」按钮，点击跳转至登录页。
7. 未登录时不展示头像、姓名、手机号等个人信息。

---

## 4.14 非功能性需求

### 4.14.1 性能要求

| 指标               | 目标    | 说明         |
| ------------------ | ------- | ------------ |
| 机构码校验响应时间 | ≤ 300ms | P95 响应时间 |
| 登录响应时间       | ≤ 500ms | P95 响应时间 |
| 页面加载时间       | ≤ 2s    | 首屏加载完成 |

### 4.14.2 兼容性

| 项               | 支持范围            |
| ---------------- | ------------------- |
| 小程序平台       | 支付宝小程序        |
| 支付宝小程序版本 | ≥ 10.0              |
| 操作系统         | iOS 12+、Android 6+ |

### 4.14.3 安全与合规

1. 注册页面需展示《用户协议》《隐私政策》链接。
2. 明确告知手机号用途：**该手机号用于账号注册和订单通知**。
3. 所有数据传输使用 HTTPS；不存储密码（无密码体系）；身份证号不持久化原文，仅存核身流水号。
4. 符合《个人信息保护法》《民法典》等法规要求。

### 4.14.4 可观测性

1. 注册、登录、实名认证、签约等关键链路必须记录审计日志，包括：时间戳、用户标识、操作类型、结果状态、错误码。
2. 关键指标需上报监控系统（打点）：注册转化漏斗、核身成功率、各错误码出现次数与频率。

---

## 4.15 MVP 验收标准

- 无机构码无法进入注册流程
- 机构码校验失败时展示明确错误提示
- 注册成功即绑定业务员，邀请关系不可覆盖
- 同一手机号禁止重复注册（同一 openId 只能注册一次）
- 已注册用户打开小程序，凭本地 token 直接进首页；token 失效跳登录页，点一次「授权登录」即可恢复
- 支付宝授权手机号一键完成注册，无需填写验证码或密码
- 所有异常均有明确的用户提示文案
- 登录、注册链路响应时间 ≤ 500ms
- 实名认证通过支付宝开放认证完成，拦截未成年人
- 课程列表与详情可正常访问
- 订单创建流程可走通（先学后付/立即付款）
- 订单列表与详情可正常查看
- 学习页展示章节列表骨架，章节点击提示「视频功能即将上线」
- 签约按钮展示，点击提示「签约功能即将上线」
- 个人中心展示用户信息、实名状态，退出登录正常

---

# 5. Iteration 2 — 核心功能完善

## 5.1 电子签约（支付宝核身确认）

**对接方案**

- 后端调用 `alipay.user.certify.open.initialize` → `alipay.user.certify.open.certify` 生成 `certifyUrl`
- 前端跳转至 `certifyUrl`，用户在支付宝内完成人脸核身作为"本人确认签约"
- 核身成功后，后端调用 `alipay.user.certify.open.query` 确认结果，订单状态更新为 ACTIVE，生成分期计划

**接口**

| 接口                                   | 说明                         |
| -------------------------------------- | ---------------------------- |
| `POST /orders/:orderId/sign/initialize` | 获取 certifyUrl，前端跳转    |
| `POST /orders/:orderId/sign/confirm`    | 查询核身结果，通过则激活订单 |

**验收条件**

1. 订单详情页 CREATED 状态下，「签约授权」按钮点击后跳转支付宝核身页面。
2. 核身通过后，订单状态更新为 ACTIVE，签约按钮消失，「去学习」按钮出现。
3. 核身失败时展示错误提示，可再次发起签约。

## 5.2 视频播放

**对接方案**

- 后端新增 `GET /api/v1/courses/videos/<videoId>/url` 接口，返回点播服务签名播放地址（防止直链泄露）
- 前端播放区域替换为支付宝原生 `<video>` 组件，传入签名 URL
- 试学模式（trial=1）：仅展示第一节，顶部横幅提示「试学模式 · 仅展示第一节课程」
- 完整模式：自由切换章节

**验收条件**

1. 点击章节可正常播放视频。
2. 试学模式仅展示第一节，底部提供购课引导。
3. 视频地址从后端动态获取（带签名鉴权，有效期可配置）。
4. 视频播放失败时展示错误提示，提供重试按钮。

---

# 6. Iteration 3 — 激励完善

## 6.1 人脸打卡激励

方案详见 § 4.11.3。

---

# 7. 页面路由规划

| 路径                       | 页面名称         | 迭代    |
| -------------------------- | ---------------- | ------- |
| /pages/index/index    | 首页（课程列表） | Iter1   |
| /pages/guide/index    | 引导页           | Iter1   |
| /pages/auth/register  | 注册页           | Iter1   |
| /pages/auth/realname  | 实名认证         | Iter1   |
| /pages/course/detail  | 课程详情         | Iter1   |
| /pages/order/confirm  | 下单确认         | Iter1   |
| /pages/order/list     | 订单列表         | Iter1   |
| /pages/order/detail   | 订单详情         | Iter1   |
| /pages/learning/index | 学习中心（视频） | Iter1\* |
| /pages/profile/index  | 个人中心         | Iter1   |

> \* Iter 1 为视频占位页，Iter 2 补全播放逻辑。
